# Serveur HTTP
PORT=3000
HOST=0.0.0.0

# MongoDB (local ou Atlas)
MONGODB_URI=mongodb://127.0.0.1:27017/dbl_tournois

# Clé d'administration (endpoints /admin/*). Vide = aucune protection (dev only).
ADMIN_KEY=
